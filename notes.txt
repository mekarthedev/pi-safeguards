# TODO

- what "ask" should be replaced with in non-interactive mode?
- protect specific line numbers, especially with temporary safeguards from the skill
    - "edit:L17-40": "ask", "edit:!L45-100": "deny"
    - how to keep track of line numbers after a few edits?
- "ask" - make llm explain, why it needs the access
- rules per scm branch
- when project-level config present, should "." & ".." be relative to project root and not cwd (like gitignore) ?
- os.homedir() vs os.userInfo().homedir
    - `sudo -u foo pi` - who's config should be used?
    - protect other users with "~/../**:!~/**": "deny!" ?
    - "~" for $HOME vs "~~" for effective system user home
- {edit: "deny", read: "allow"} vs "sed -i" - which permission wins?
- absolute paths on windows:
    - either recognize "C:/**" pattern as absolute or require users to write "/C:/**"
- known non-path arguments: "x": { "*": "ask" } shouldn't trigger on "echo x" or "git x"
    - cd, basename/dirname, echo, printf, tar czvf
    - git $cmd, npm $cmd, etc
        - heuristic: ignore first positional if it's just \w+, except for known tools: `read id_rsa`, `rm -rf dir`
    - find ("find $1:!find * $1")
- order-dependent opts in tool matching: e.g. grep --include vs --exclude
    - `grep [.]--include= [.]--exclude=` as analogy to `dd [--]if= [--]of=`
- support capturing CWD-args in tool pattern: `git ... # [-C=$cwd]` or `git -C=$cwd`
- rules per CWD: e.g. deny launching subagents outside CWD
- harness session CWD vs temporary bash script CWD:
    - "./id_rsa": "allow" vs `cd ~/.ssh && cat ./id_rsa` - should path pattern refer to ctx.cwd or to cwdSimulation.cwd ?
- inspect alternative bash execution paths:
    - `(cd a && cp || cd b && mv) && rm` -> [cp@a, mv@b, rm@a, rm@b]
- concurrent call of multiple tools - check if rule resolutions displayed correctly for each
- variadic num of paths in a single arg: `java -cp lib/a.jar:lib/b.jar:lib/c.jar`
- hot-reload config
- report malformed tool patterns: "sort (*) -o=$1" is effectively "sort -o=$1 (*)", which never matches
- command for ad-hoc in-session rules: `/safeguard deny grep * *:!~/*/**`

# Pitch

- rule: "path-pattern:!except-path-pattern": { "tool-pattern:!except-tool-pattern": "permission[!]" }
    - "read" also affects:
        - cp(from), mv(from), <, grep, rg, cat, tail, head, dd(if=), sed, cut, uniq(input), sort(input)
    - "edit" also affects:
        - "write" (only itself)
        - cp(to), mv(to), mkdir, >, >>, dd(of=), sed(-i), uniq(output), sort(--output), truncate, tee, touch
    - "write" is same as "edit", but also affects:
        - rm, mv(from)
    - shorthand syntax:
        - "path": "permission" = "path": { "*": "permission" }
        - "nowrite" = "readonly" = { "write": "deny" }
            - case: {"../": "deny", ".git/": "readonly"} might give unexpected result for "../.git"
        - "askwrite" = { "write": "ask" }
        - "noedit" = { "edit": "deny" } - note: same? as { "write:!bash mv:!bash rm"}
        - "askedit" = { "edit": "ask" }
        - "noread" = "writeonly" = { "read": "deny" }
    - exceptions: ".ssh/**:!*.pub": "deny" - meaning this rule should be skipped for "*.pub"
    - rules are checked from bottom to top, first rule (per targeted path) with matching path and tool wins
    - "permission!" makes rule both high priority and strict, e.g. "pi-safeguards.json": "readonly!"
        - high priority = the rule is checked before any non-high priority rule
        - strict = matching doesn't stop: all strict rules are checked -> most strict wins
        - to prevent unintetional overriding by e.g. ".." or "*.json"
        - full syntax: "a": { "b": "allow!", "c": "deny!" }
    - if no rule for the tool, then go to next path pattern
    - if no matchin rule -> default pi behavior = "allow", or some other extension
    - "edit": "ask" - when asking, present the user with the proposed diff
        - an option to open diff in external tool
- path pattern:
    - "*" for exactly one path segment, "**" for zero or more path segments, "?" for exactly one char
        - "*" is zero or more characters, including leading dots (unlike bash)
            - "/*/" requires at least one character
        - "xxx/**" includes "xxx" itself
        - "xxx/**.x" is unsupported, use "xxx/**/*.x" instead
        - "**/xxx" is equivalent to "xxx" (except cases like **/.. and **/~)
    - trailing `/` -> do "dir check": don't match if the path points to a non-directory, otherwise -> don't do dir check
        - "dir/" matches only "dir", and not its children (unlike gitignore, use "dir/**")
        - "dir/**" matches anything within dir, but matches dir itself only if its not a non-directory
        - if path doesn't exist -> assume it matches the dir check
            - for some bash commands assume type based on the command:
                - mkdir -> directory, cat -> non-directory
        - e.g. "dir/**:!dir/**/" - match only non-directories within "dir"
        - e.g. { "dir/": "readonly", "dir/**/*": "allow" } - can't touch the folder, but can touch its contents
    - "." & ".." for cwd, "~" for home, "/" for fs root (unlike gitignore)
        - "./xxx" is not the same as "xxx" (which is equivalent to "**/xxx")
        - "xxx/../../y" makes it "y", not "../y"
    - "\\*" for escaping
- exec/bash: "command-pattern:!except-pattern": { "subcommand-pattern": "permission" }
    - command patterns are matched structuraly
    - `cmd && cmd || cmd` - every cmd is processed separately
    - track cwd in case of `cd some && cd where && bash`
- (quasi-)security / safeguards for safegards:
    - config file is "readonly!" by default
    - next rule overrides previous rule, even if previous is stricter
    - when "bash" and "paths" both match -> stricter wins
    - project vs user config:
        - by default: stricter rule wins (e.g. running with untrusted 3rd party config)
        - /safeguards:trust command to allow project config to override parent/user config, normal priority only
- config lint:
    - "*" pattern, if present, must be the first one in the list, and must not be of high priority
    - "sed *:!sort *" - mismatching command names -> exception does nothing
    - /safeguards:lint for additional checks:
        - check "xxx/**" is after "xxx/"
- "safegards help steering" philosophy
    - guaranteeing strict access controll, i.e. sandboxing, is a non-goal
        - llm accidentally bypassing safeguards is not the end of the world as long as it's reminded of the rules in time
    - explain to the agent what is it allowed to do
    - a skill to allow llm make temporary safeguards for itself.
        - e.g. when prompting like "safeguard implementation files from changes, only write tests"
- non-goals:
    - sandboxing
    - preventing home spoofing: $HOME vs actual process user home
    - preventing accidents in custom scripts. Will only check paths used in tool calls
    - recursively checking subfolders for e.g. rm -rf. Will only check explicitly mentioned paths
- caveats:
    - dir-check for non-existing paths always matches
    - home is the same home as for the user config json, not the actual system user home


# History

## Simplified pattern matching

Pattern: | Old behavior:      | New behavior:      | New equivalent for old behavior:
x/       | x(d) or x/y/z(f|d) | x(d)               | x/**
x/**     | x/y/z(f|d)         | x(d) or x/y/z(f|d) | x/**/*
x        | x(f)               | x(f|d)             | x:!*/
*:!*/    | - (see old x/)     | x(f) or y(f)       | -
x/**/    | x/y(d) or x/y/z(d) | x(d) or x/y/z(d)   | x/**/*/


## Dir check for non-existent paths

A tool is called with "x" path, type is unknown, e.g. bc it doesn't exist.
Should the following rules match or skip the path for `cmd x`?

pattern   |           ideally               |                    implementation
    input | node_modules | package.json     | something[/] (dir-like)  | something.json (file-like)
x/        |  match       |  skip (notice /) |  match(#)(1)             |
*:!x/     |  skip        |  match           |  skip(#)(1)              |  
*:!x/**   |  skip        |  match           |  skip(#)(1)              |
*/        |  match       |  skip            |  match(#)/skip(!)        |  skip(#)
*:!*/ (?) |  skip        |  match           |  skip(#)                 |
x:!*/     |  skip?       |  match           |  match(!)(2)             |  match(#)

(!) - strict dir check = only if actually dir
(#) - guess dir check = only if at least guessed to be dir
(1) - user wrote "something/" pattern -> something is expected to be a possible dir
        - vs "it could be either"
(2) - x mentioned explicitly
(?) - "nowrite" = "can only create files" vs ???

For some actions type of path is known even if path doesn't exist:
- must be file: read, write, edit, cp/rm (without -r), touch
- must be dir: mkdir

Use heuristic for name: `.+\.(?!d$)[a-zA-Z]+$` and no trailing-/ -> guess it's a file, otherwise guess dir

Spawn subagent to ask it if the path is expected to be a directory or a file?

Solution (basically postpone the problem):
- Remove "strict" mode. Remove "unknown" type.
- Guess type outside of pattern matching logic. Guess by name. Make a part of other guessing logic (guessing by tool used).

Make match return true|false|unknown

match->unknown only possible if (a) dir check was requested and (b) target type is unknown (noent)

Situations for unknown result:
1. main regex match, main required dir check, all exceptions regexes didn't match
   - x/       | match(2)
   - x/:!*.y  | match(2)
2. main regex match, no main dir check, exception regex matched but required dir check
              | something.json | something[/] | no strict checks solution
   - x:!x/    | match          | match(1)     | skip
   - x:!x/**  | match          | match(1)     | skip
   - x:!*/    | match(2)       | match(1)     | skip
   - *:!x/    | skip(2)        | skip(2)      | 
   - *:!x/**  | skip(2)        | skip(2)
   - *:!*/    | match(1)       | match(1)

(1) - just in case
(2) - not a wildcard -> in practice it's a name for a known type

Examples:
- "node_modules/**": "ask", "*:!node_modules/**": "deny". Then rm -rf node_modules
- "package.json:!*/": "ask". Then rm -rf package.json - note that file doesn't exist
- "*": "deny", "notes.txt:!*/": "allow" -> files cannot be created
- If guessing by name: "*": "deny", "*:!f.txt/": "allow". Then mkdir f.txt -> guessed f.txt to be a file -> created "f.txt" dir despite "deny"

non-existent paths can only be created = nothing(?) to loose when skipping rules with dir-checked exceptions

Solution: No strict dir check. "*:!*/" always skipped unless known to be a file

For "Caveats" section:
- */ may not check if the path is actually a directory when the path in question doesn't exist, making rules like "xxx:!*/" be skipped if xxx doesn't exist


## [Outdated] Captured value identity

Problem:
- `rm $1:!rm * $1` vs "rm f f"
- `rm $1 $1:!rm * $1` vs "rm f f" ???
- `sort $1:!sort -o=$1 * $1` vs "sort f f -o f"

`convert $1:!convert * $1` vs "convert inplace.txt inplace.txt" (hard to find a real world example)
- must be a way to distinctly capture the read path vs the write path

cmd -x=$1 :! cmd $1  - value of -x that is not a positional
cmd -x=$1 $1 :! cmd $1  - contradicting
cmd -x=$1 $1 :! cmd * $1 *  - both value of -x and positional that is not a middle positional
cmd $1 :! cmd -y=$1  - any positional that is not a value of -y
cmd $1 :! cmd -y=$1 * $1  - any positional that is not (value of -y and non-first)
cmd -x=$1 :! cmd -x=$1 * * $1  - any value of -x that is not (value of -x and a positional starting from 3rd)
cmd -x=$1 :! cmd -y=$1  - values of -x that are not values of -y
cmd -x=$1 -y=$1 :! cmd -y=$1  - contradicting
cmd -x=$1 $1 :! cmd -y=$1 $1  - values of -x that are also positionals but not values of -y that are positionals

`cmd -x=$1 :! cmd -x=$1 * * $1` vs "cmd f f f -x f" or "cmd f f f -x f -x f"
- `cmd -x=$1 $1 :! cmd -x=$1 * * $1`  - want to interpret like redundant -x= in exception
- `cmd -x=$1 :! cmd -y=$1`
- `cmd -x=$1 $1 :! cmd -y=$1`
- `cmd -x=$1 $1 :! cmd -y=$1 $1`

main \ exception    positional         opts                positional + opts
positional          $1 :! * $1         $1 :! -y=$1         $1 :! -y=$1 * $1
opts                -x=$1 :! * $1      -x=$1 :! -y=$1      -x=$1 :! -y=$1 * $1
positional + opts   -x=$1 $1 :! * $1   -x=$1 $1 :! -y=$1   -x=$1 $1 :! -y=$1 * $1

if captured positionals in exception -> exclude positionals, check if empty
otherwise -> exclude value
-> whether value was captured by opts doesn't even matter


## Position matching for wildcarded vs literal positionals

Problem:
- `dd of=$1` - should it require fixed position like literals or match any position like wildcard?
- `git * $1` - hard to describe "ignore sub-command"

alternative:
- `cmd $1` / `cmd *` - match first positional, not any positional, just like for literals
    - `cmd $1*` / `cmd **`
- `cmd **` - match zero or more positionals
- `cmd ** $1` - capture any positional
- `cmd $1;` - same as `$` in regex

Should `cmd ** $1` be equivalent to `cmd ** $1 **` ?
- "git branch": "deny" must match `git branch x y`

- `dd ** of=$1`
- `git * ** $1`
- `cp ** $1 ;` - only destination
- `cp ** $1 *` - any source


## Identity of positional substring capture

Problem:
"rm $1 :! rm $1 sub/$1" vs `rm x sub/x` -> should exclude "sub/x"?
- exclude substrings by value -> keep "sub/x" -> somewhat unintuitive
- no way to describe "all positionals except those that match pattern x"
  - compare to "except those that are non-first/non-last"

Solution:
- any presence of $1 marks positional for exclusion by index
- captured substring (not full value) is used for exclusion by value
- only compare substring to substring or full value, never full to full
- support "$1*" or "*$1" to allow using full value as substring

main capture         exception capture                         decision
["sub/x" @ n, none] - ["sub/x" @ n, none] -> n == n              -> exclude (by index, as before)
["sub/x" @ n, none] - ["sub/x" @ m, none] -> n != m              -> keep (by index, as before)
["of=x" @ n, "x"]   - ["if=x" @ m, "x"]   -> "x" == "x"          -> exclude (by value)
["sub/x" @ n, none] - ["sub/x" @ n, "x"]  -> n == n              -> exclude (by index, similar to before)
["sub/x" @ n, "x"]  - ["sub/x" @ n, none] -> n == n              -> exclude (by index, similar to before)
    - `rm sub/$1 :! rm * $1 *` vs "rm a sub/x c"
["sub/x" @ n, "x"]  - ["x" @ m, none]     -> n != m, no substr   -> keep (by value)
    - `rm sub/$1 :! rm * $1 *` vs "rm sub/x x sub/y sub/z"  -> ["sub/x", "sub/z"]
["sub/x" @ n, "x"]  - ["x" @ m, "x"]     -> "x" == "x"           -> exclude (by value)
    - `rm sub/$1 :! rm * $1* *` vs "rm sub/x x sub/y sub/z" -> ["sub/z"]
["x" @ n, none]     - ["sub/x" @ m, "x"]  -> full "x" == sub "x" -> exclude (by value)

- same idx -> same full value
- different idx -> might be same full value

(m - main, e - exception, i - index, f - full value, s - substring, none - no substring)
substr \ indexes    |  im != ie, fm != fe        |  im != ie, fm == fe          |  im == ie (fm == fe)
---------------------------------------------------------------------------------------------------------
- none, none        |  "$1:!* $1",`x y`          |  "$1:!* $1",`x x`            |  "$1:!* $1",`z y`
                    |  x vs y -> keep            |  x@0 vs x@1 -> keep          |  y@1 vs y@1 -> exclude

- sm == fe, none    |  "s/$1:!* $1",`s/x x`      |  "$1*:!* $1",`x x`           |  "$1*:!* $1",`z y`
                    |  x@0 vs x@1 -> keep (1)    |  x@0 vs x@1 -> keep          |  y@1 vs y@1 -> exclude

- sm != fe, none    |  "s/$1:!* $1",`s/x y`      |  "s/$1:!* $1",`s/x s/x`      |  "s/$1:!* $1",`z s/y`
                    |  x@0 vs y@1 -> keep        |  s/x@0 vs s/x@1 -> keep      |  y@1 vs s/y@1 -> exclude

- none, fm == se    |  "$1:!* s/$1",`x s/x`      |  "$1:!* $1*",`x x`           |  "$1:!* $1*",`z y`
                    |  x@0 vs x@1 -> exclude (0) |  x@0 vs x@1 -> exclude       |  y@1 vs y@1 -> exclude

- none, fm != se    |  "$1:!* s/$1",`x s/y`      |  "$1:!* s/$1",`s/x s/x`      |  "$1:!* s/$1",`z s/y`
                    |  x@0 vs y@1 -> keep        |  s/x@0 vs x@1 -> keep        |  s/y@1 vs y@1 -> exclude (2)

- sm == se          |  "a=$1:!b=$1",`a=x b=x`    |  "a=$1:!* a=$1",`a=x a=x`    |  "a=$1:!* a=$1",`z a=x`
                    |  x@0 vs x@1 -> exclude (3) |  x@0 vs x@1 -> exclude (4)   |  x@1 vs x@1 -> exclude

- sm != se          |  "a=$1:!b=$1",`a=x b=y`    |  "a=$1:!* a=/$1",`a=/b a=/b` |  "a=$1:!a=/$1",`z a=/b`
                    |  x@0 vs y@1 -> keep        |  /b@0 vs b@1 -> keep         |  /b@1 vs b@1 -> exclude (2)

(0) - an issue: "except equal to this substring" and "except matching this pattern" are combined into one feature
(1) - feature: exclude complex patterns by position index
(2) - feature: way to say "except positionals that match pattern x"
(3) - "dd of=$1 :! dd if=$1"
(4) - see (3), use (1) instead: "a=$1 :! * $1"

Why exclusion even exist?
- alternative pattern syntax can handle cases like "only n-th positional"
- "sort $1 :! sort -o=$1": "deny" vs `sort a b c -o=b` - deny reading without overwriting

More problems:
- Separate "equal to $1 in of=/tmp/$1" from "match of=/tmp/$*"
- How can be applied to opts?
- "docker -v ${/secrets/*}:*" - capture substring that matches pattern, including pattern part

Solution:
- "equal to $1 in of=/tmp/$1" -> "dd of=/tmp/(*)"
- "match of=/tmp/$*" -> "dd of=(/tmp/*)"
- "echo $1 :! echo ultra-$1" vs `echo ultra-fox jumps over fox`
    -> "echo (*) :! echo (ultra-*)" for ["jumps", "over", "fox"]
    -> "echo (*) :! echo ultra-(*)" for ["ultra-fox", "jumps", "over"]
    -> "echo (*) :! echo ultra-(*) :! echo (ultra-*)" for ["jumps", "over"]
- "docker -v (/secrets/*):*"
- reserve "$1" only for backreferences
    - "sort (*) --output=$1"

"echo (*) :! echo ultra-(*)" vs "echo (*) :! echo ultra-$1"
- "echo (*) :! echo (ultra-*)" is a thing even with allowed cross-exception backrefs.
